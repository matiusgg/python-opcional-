# NUNCA PERO NUCA SE METE UN HTML EN UNA CARPETA, la unica opcion seria hacer una carpeta idioma, donde iran los html's con diferentes idiomas. DE RESTO NUNCA.
# <!-- Para llamar en HTMl al CSS en python hacemos esto: Donde {{url_for(Nos buscara la direccion)('static'(la carpeta que creamos), filename='direccion del CSS')}}-->
# ESTE CODIGO NOS GENERADA UNA ID O UN LOCALHOUST POR ASI DECIRLO.

# Importamos la funcion de FLASK de la carpeta flask
# Para usar el archivo HTML, importamos tambien RENDER: DOnde render_NombreCarpete(template)
from flask import Flask, render_template

# Creamos una variable donde guardar una instacia(objeto) de lfask

app = Flask(__name__)

# Creamos una ruta en root

# Donde creamos un decorador. Endonde ROUTE, hace parte del objeto FLASK, osea la variable app

@app.route('/')
def home():
    # RETORNAMOs la FUncion RENDER donde escribimos el nombre de la carpeta.
    return render_template('home.html')

# Si queremos crear directorios/rutas dentro del servidor, le ponemos un nuevo nombre a @app.route('/nombreRuta)

@app.route('/about')
def about():
    return render_template('about.html')



# Generamos el punto de entrada en nuestro proprama con if MAIN

if __name__ == "__main__":
    # RUn: Es una funcion de FLASK: la cual sirve para abrir la aplicacion
    # app.run()

    # aL EJECUTRALO NOS SALDRA UNA URL QUE LA PONDREMOS ENE L NAVEGADOR Y ESTO SERIA COMO EL MAMP Y EL XAMPP
    # El problema viene en que si cambiamos el contenido que vamos a crear, para actualizarlo tenemos que volver a poner RUN PYTHON, lo cual crea un problema
    # porque no se actualiza solo
    # Para solucionarlo, ponemos en el RUN debug=True,
    # para salir en la consola usa mos CONTROL + c
    # Creamos una carpeta en site-packages llamada templates, creamos un archivo llamado home.html

    app.run(debug=True)

    # Ahora creamos una caprte en site-packages llamada STATIC: Vamos a meter el CSS, el JAVA-SCRIPt, cualquier tipo de tema estetico